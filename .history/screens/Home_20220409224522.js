import { View, SafeArea, FlatList, Text } from "react-native";
import {COLORS , NFTData} from "./constants";
const Home = () => {
  return (

    <SafeAreaView>
      
    </SafeAreaView>
  )
};

export default Home;
