import { View, SafeArea, FlatList, Text } from "react-native";
import { COLORS, NFTData } from "../constants";

import {NFTCard, HomeHeader, FocusedStatusBar} from ""
const Home = () => {
  return <SafeAreaView style={{ flex: 1 }}></SafeAreaView>;
};

export default Home;
